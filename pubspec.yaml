name: personal_finance_app
description: "Privacy-first, offline-capable personal finance app with AI coaching"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # Core Architecture
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  collection: ^1.18.0
  freezed_annotation: ^2.4.4

  # Database & Storage
  drift: ^2.18.0
  sqlcipher_flutter_libs: ^0.5.1
  path_provider: ^2.1.3
  path: ^1.9.0
  
  # Security
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.2.0
  
  # Notifications
  flutter_local_notifications: ^17.2.2
  
  # UI & Utilities
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  http: ^1.2.1
  shared_preferences: ^2.3.2
  currency_converter: ^3.0.0
  share_plus: ^10.1.2
  
  # CSV Export
  csv: ^6.0.0
  
  # Encryption
  encrypt: ^5.0.3
  crypto: ^3.0.3
  
  # Logging
  logging: ^1.2.0
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Code Generation
  build_runner: ^2.4.11
  riverpod_generator: ^2.3.5
  drift_dev: ^2.18.0
  freezed: ^2.4.7
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
